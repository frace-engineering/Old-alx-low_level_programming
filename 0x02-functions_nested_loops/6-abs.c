#include <stdlib.h>
#include "main.h"


/**
 * _abs - prints the absolute value on an integer
 * @n: the integer value to work on
 * Return: absolute value on int n (Success)
 */

int _abs(int n)
{
	return (abs(n));
}

