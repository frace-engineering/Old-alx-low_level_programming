#include "main.h"

/**
 * _memcpy - does nothing
 * @src: pointer to source
 * @dest: string pointer
 * @n: number
 *
 * Return: str (Success)
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
